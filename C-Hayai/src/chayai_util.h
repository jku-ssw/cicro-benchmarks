#ifndef CHAYAI_UTIL_H
#define CHAYAI_UTIL_H

// get the path were temporary files should be stored. Those files need to be removed by the benchmark itself!
char *chayai_util_get_tmp_dir();

#endif // CHAYAI_UTIL_H
